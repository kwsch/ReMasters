syntax = "proto3";
package ReMastersLib;

message DeckPresetGroup {
	int32 deck_preset_group_id = 1;
	int32 deck_preset_id = 2;			// DeckPreset.pb
}

message DeckPresetGroupTable {
	repeated DeckPresetGroup entries = 1;
}
